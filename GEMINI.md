# Project Instructions & Rules

- **Never commit before build**: Always ensure the project builds successfully and compilation/tests pass before creating or proposing any git commit.
- **Produce package after commit**: After committing, produce a new package using the build + publish pipeline.
